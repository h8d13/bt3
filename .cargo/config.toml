# Align all function entry points to a 64-byte cache line boundary.
# Prevents layout shifts in .text from causing benchmark noise when
# any function changes size.
[build]
rustflags = ["-C", "llvm-args=-align-all-functions=64"]
